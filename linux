#Coringas

"*" //Faz referência a um nome completo/restante de um arquivo/diretório.
"?" //Faz referência a uma letra naquela posição.

#Exemplos de comandos

ls *.txt //Pega todos os arquivos que começam com qualquer nome e terminam com .txt
ls teste?.new //Lista todos os arquivos que começam com teste, contenham qualquer caracter na posição do coringa ? e terminem com .new

#Comandos que eu achei dahora

echo $PATH //Caminho 
uname -a //Mostra a versão do sistema
date //Mostra a hora e a data 
fdisk 
cut //Corta o conteúdo 
less //Paginador
